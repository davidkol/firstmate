projects/
state/
data/
.no-mistakes/
.lavish/
.fm-secondmate-home
.DS_Store
__pycache__/
*.pyc
.env
# The whole directory, per AGENTS.md section 2: config/ is captain-private local
# operating choices, none of it tracked. Enumerating the files instead let a new
# one (config/startup-memory-budget) land unignored and dirty every home.
config/
